query ControllerData {
    controllerEntities @namedClient(name: "SONIC_GAME_SUBGRAPH"){
        id
        dungeonFactory
        gameToken
        gameObjectController
        reinforcementController
    }
}