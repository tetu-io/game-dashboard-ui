query UserStatData($first: Int!, $skip: Int!) {
    userStatEntities(
        first: $first
        skip: $skip
    ) @namedClient(name: "SONIC_GAME_SUBGRAPH") {
        id
        heroes
        items
        earned
        actions
        pawnshopActions
    }
}