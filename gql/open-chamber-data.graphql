query OpenChamberData($first: Int!, $skip: Int!, $orderDirection: OrderDirection!) {
    openedChamberEntities(
        where: {
            chamber_: { isBattle: true }
        }
        first: $first
        skip: $skip
        orderBy: timestamp
        orderDirection: $orderDirection
    ) @namedClient(name: "FANTOM_GAME_SUBGRAPH") {
        id
        dungeon {
            id
        }
        enteredHero {
            id
            heroId
            meta {
                id
                heroClass
            }
        }
        completed
        stage
        iteration
        chamber {
            id
        }
        actions(
            orderBy: turn
            orderDirection: desc
            first: 1
        ) {
            completed
            kill
        }
        timestamp
    }
}